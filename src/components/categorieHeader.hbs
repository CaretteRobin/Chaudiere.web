<div class="bg-white p-6 rounded-xl shadow-lg text-center mb-8">
  <h1 class="text-3xl font-bold text-indigo-700 mb-2">{{name}}</h1>
  {{#if description}}
    <p class="text-lg text-gray-700">{{description}}</p>
  {{else}}
    <p class="text-gray-400 italic">Aucune description disponible.</p>
  {{/if}}
</div>
