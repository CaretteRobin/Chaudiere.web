<div class="max-w-2xl m-8 border-[1px] border-gray-200 rounded-lg p-6 shadow-md">
    <div class="">
        <h2 class="text-2xl font-bold mb-4">{{title}}</h2>
        <div class="space-y-4">
            <div class="flex items-center">
                <span class="font-semibold w-24">Date:</span>
                <span>{{start_date}}</span>
            </div>
            <div class="flex items-center">
                <span class="font-semibold w-24">Catégorie:</span>
                <span class="text-gray-600">{{category}}</span>
            </div>
            <div class="flex items-center">
                <a href="{{ url }}" class="text-indigo-600 hover:underline" data-link>Voir plus</a>
            </div>
        </div>
    </div>
</div>