<div class="max-w-4xl m-auto">
    {{{categHeader}}}
    {{#if eventsContent}}
        {{{eventsContent}}}
    {{else}}
        <h2 class="text-2xl font-bold mb-4">Aucun événement trouvé dans la catégorie</h2>
    {{/if}}
</div>
